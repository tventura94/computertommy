---
import astroLogo from '../assets/computertommylogo.svg';
import background from '../assets/background.svg';
---

<div id="container">
	<img id="background" src={background.src} alt="" fetchpriority="high" />
	<main>
		<!-- Hero: one clear focal block -->
		<header class="hero">
			<a href="/" class="site-name">
				<img src={astroLogo.src} width="115" height="48" alt="" />
			</a>
			<div class="hero-content">
				<h1 class="hero-title">I build simple, clean websites for local businesses that don't want to deal with agencies.</h1>
				<p class="hero-subtitle">
					One-time build. No monthly fees. Real dude.  Easy process. You own the site.<br />
				</p>
				<div class="hero-cta">
					<a href="/contact" class="btn btn-primary">Tell me what you need →</a>
					<a href="/portfolio" class="btn btn-secondary">Sites I've built</a>
					<a href="/about" class="btn btn-secondary">About</a>
				</div>
			</div>
		</header>

		<!-- Sections: clear blocks with labels -->
		<section class="block block-process" id="how-it-works">
			<span class="block-label">How it works</span>
			<h2 class="block-title">Five steps. No runaround.</h2>
			<div class="process-card">
				<ol class="process-list">
					<li><span class="step-num">1</span> You message me</li>
					<li><span class="step-num">2</span> We talk for 10 minutes</li>
					<li><span class="step-num">3</span> I quote it</li>
					<li><span class="step-num">4</span> I build it</li>
					<li><span class="step-num">5</span> You own it</li>
				</ol>
			</div>
		</section>

		<section class="block block-pricing" id="simple-pricing">
			<span class="block-label">Pricing</span>
			<h2 class="block-title">Simple pricing</h2>
			<div class="pricing-card">
				<p class="pricing-lead">Most small business sites land around <strong>$300–$600</strong> depending on what you need.</p>
				<p class="pricing-note">I'll tell you the price upfront. No subscriptions, no surprise invoices.</p>
			</div>
		</section>

		<section class="block block-about" id="about-tom">
			<span class="block-label">Who you're talking to</span>
			<h2 class="block-title">Hi, I'm Tom.</h2>
			<div class="about-card">
				<div class="about-photo">
					<img src="/tom.jpg" alt="Tom" loading="lazy" />
				</div>
				<div class="about-text">
					<p class="about-lead">Bass player. Local. I build sites for bands and small businesses.</p>
					<p>I hate monthly fee traps. I use modern tools (Netlify, etc.) and get you a clean site with quick turnaround. You own it when we're done.</p>
					<p>If that sounds like what you're looking for, <a href="/contact">tell me what you're trying to build</a> and we'll go from there.</p>
				</div>
			</div>
		</section>

		<section class="block block-why" id="why-not-builders">
			<span class="block-label">Why not a builder?</span>
			<h2 class="block-title">Why not just use Wix or Squarespace?</h2>
			<div class="why-grid">
				<article class="why-card">
					<h3>No monthly fees</h3>
					<p>Pay once, own your site. No subscription eating into your budget.</p>
				</article>
				<article class="why-card">
					<h3>Clean, fast code</h3>
					<p>No bloated drag-and-drop output. Just a simple site that loads fast.</p>
				</article>
				<article class="why-card">
					<h3>No learning curve</h3>
					<p>You don't have to figure out the builder. Tell me what you need; I build it.</p>
				</article>
			</div>
		</section>
	</main>
</div>

<style>
	#background {
		position: fixed;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		z-index: -1;
		filter: blur(100px);
	}

	#container {
		font-family: Inter, Roboto, 'Helvetica Neue', 'Arial Nova', 'Nimbus Sans', Arial, sans-serif;
		min-height: 100%;
		max-width: 720px;
		margin: 0 auto;
		padding: 0 1.5rem;
	}

	main {
		padding: 0 0 4rem 0;
	}

	/* ----- Hero ----- */
	.hero {
		padding: 3rem 0 4rem;
		border-bottom: 1px solid rgba(0, 0, 0, 0.08);
	}

	.hero .site-name {
		display: inline-flex;
		align-items: center;
		gap: 0.75rem;
		text-decoration: none;
		margin-bottom: 2.5rem;
		transition: opacity 0.2s ease;
		opacity: 0.95;
	}

	.hero .site-name:hover {
		opacity: 1;
	}

	.site-name-text {
		font-size: 1.25rem;
		font-weight: 600;
		color: #0f172a;
		letter-spacing: -0.02em;
	}

	.hero-content {
		max-width: 640px;
	}

	.hero-title {
		font-size: clamp(1.75rem, 4.5vw, 2.5rem);
		font-weight: 700;
		line-height: 1.25;
		letter-spacing: -0.02em;
		color: #0f172a;
		margin: 0 0 1.25rem 0;
	}

	.hero-subtitle {
		font-size: 1.125rem;
		line-height: 1.6;
		color: #64748b;
		margin: 0 0 2rem 0;
		max-width: 540px;
	}

	.hero-cta {
		display: flex;
		flex-wrap: wrap;
		gap: 0.75rem;
	}

	.btn {
		display: inline-block;
		padding: 0.75rem 1.5rem;
		font-size: 1rem;
		font-weight: 500;
		text-decoration: none;
		border-radius: 8px;
		transition: transform 0.15s ease, box-shadow 0.15s ease;
	}

	.btn:hover {
		transform: translateY(-1px);
	}

	.btn-primary {
		background: linear-gradient(135deg, #3245ff 0%, #6366f1 100%);
		color: white;
		box-shadow: 0 4px 14px rgba(50, 69, 255, 0.35);
	}

	.btn-primary:hover {
		box-shadow: 0 6px 20px rgba(50, 69, 255, 0.4);
	}

	.btn-secondary {
		background: rgba(255, 255, 255, 0.85);
		color: #334155;
		border: 1px solid rgba(0, 0, 0, 0.1);
	}

	.btn-secondary:hover {
		background: rgba(255, 255, 255, 0.95);
		border-color: rgba(0, 0, 0, 0.15);
	}

	/* ----- Section blocks ----- */
	.block {
		padding: 3.5rem 0 0;
	}

	.block-label {
		display: block;
		font-size: 0.75rem;
		font-weight: 600;
		letter-spacing: 0.08em;
		text-transform: uppercase;
		color: #6366f1;
		margin-bottom: 0.5rem;
	}

	.block-title {
		font-size: 1.5rem;
		font-weight: 700;
		letter-spacing: -0.02em;
		color: #0f172a;
		margin: 0 0 1.5rem 0;
		line-height: 1.3;
	}

	/* Process */
	.block-process .process-card {
		background: rgba(255, 255, 255, 0.7);
		border-radius: 12px;
		padding: 1.75rem 2rem;
		border: 1px solid rgba(0, 0, 0, 0.06);
		box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
	}

	.process-list {
		list-style: none;
		padding: 0;
		margin: 0;
		display: flex;
		flex-direction: column;
		gap: 0.875rem;
	}

	.process-list li {
		display: flex;
		align-items: center;
		gap: 1rem;
		font-size: 1.0625rem;
		color: #334155;
		line-height: 1.4;
	}

	.step-num {
		flex-shrink: 0;
		width: 1.75rem;
		height: 1.75rem;
		background: linear-gradient(135deg, #3245ff 0%, #6366f1 100%);
		color: white;
		border-radius: 50%;
		display: inline-flex;
		align-items: center;
		justify-content: center;
		font-size: 0.8125rem;
		font-weight: 600;
	}

	/* Pricing */
	.block-pricing .pricing-card {
		background: rgba(99, 102, 241, 0.06);
		border: 1px solid rgba(99, 102, 241, 0.15);
		border-radius: 12px;
		padding: 1.5rem 1.75rem;
		max-width: 480px;
	}

	.pricing-lead {
		font-size: 1.0625rem;
		color: #0f172a;
		font-weight: 500;
		margin: 0 0 0.5rem 0;
		line-height: 1.5;
	}

	.pricing-note {
		font-size: 0.9375rem;
		color: #64748b;
		margin: 0;
		line-height: 1.5;
	}

	/* About Tom */
	.block-about .about-card {
		display: flex;
		gap: 2rem;
		align-items: flex-start;
		background: rgba(255, 255, 255, 0.7);
		border-radius: 16px;
		padding: 2rem;
		border: 1px solid rgba(0, 0, 0, 0.06);
		box-shadow: 0 2px 12px rgba(0, 0, 0, 0.05);
	}

	.about-photo {
		flex-shrink: 0;
		width: 160px;
		height: 160px;
		border-radius: 12px;
		overflow: hidden;
		background: #e2e8f0;
	}

	.about-photo img {
		width: 100%;
		height: 100%;
		object-fit: cover;
	}

	.about-text {
		flex: 1;
		min-width: 0;
	}

	.about-lead {
		font-size: 1.0625rem;
		font-weight: 600;
		color: #0f172a;
		margin: 0 0 0.75rem 0;
		line-height: 1.5;
	}

	.about-text p {
		font-size: 1rem;
		color: #475569;
		line-height: 1.6;
		margin: 0 0 0.75rem 0;
	}

	.about-text p:last-child {
		margin-bottom: 0;
	}

	.about-text a {
		color: #6366f1;
		text-decoration: none;
		font-weight: 500;
	}

	.about-text a:hover {
		text-decoration: underline;
	}

	/* Why not builders */
	.why-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 1.25rem;
	}

	.why-card {
		background: rgba(255, 255, 255, 0.7);
		border-radius: 12px;
		padding: 1.5rem;
		border: 1px solid rgba(0, 0, 0, 0.06);
		box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
		transition: box-shadow 0.2s ease, transform 0.2s ease;
	}

	.why-card:hover {
		box-shadow: 0 4px 16px rgba(0, 0, 0, 0.08);
		transform: translateY(-2px);
	}

	.why-card h3 {
		font-size: 1rem;
		font-weight: 600;
		color: #0f172a;
		margin: 0 0 0.5rem 0;
	}

	.why-card p {
		font-size: 0.9375rem;
		color: #64748b;
		line-height: 1.5;
		margin: 0;
	}

	/* ----- Responsive ----- */
	@media screen and (max-width: 768px) {
		#container {
			padding: 0 1rem;
		}

		.hero {
			padding: 2rem 0 3rem;
		}

		.hero .logo {
			margin-bottom: 2rem;
		}

		.hero-title {
			font-size: 1.625rem;
		}

		.hero-subtitle {
			font-size: 1rem;
			margin-bottom: 1.5rem;
		}

		.hero-cta {
			flex-direction: column;
			align-items: stretch;
		}

		.btn {
			text-align: center;
			padding: 0.875rem 1.25rem;
		}

		.block {
			padding-top: 2.5rem;
		}

		.block-title {
			font-size: 1.35rem;
		}

		.block-process .process-card {
			padding: 1.5rem 1.25rem;
		}

		.block-about .about-card {
			flex-direction: column;
			padding: 1.5rem;
			align-items: center;
			text-align: center;
		}

		.about-photo {
			width: 140px;
			height: 140px;
		}

		.about-text {
			text-align: left;
		}

		.why-grid {
			grid-template-columns: 1fr;
		}
	}

	@media screen and (max-width: 480px) {
		.hero-title {
			font-size: 1.5rem;
		}
	}
</style>
